# DataAlpha ONE landing page

Premium event landing page for Seamless Middle East 2026.

```bash
npm install
npm run dev
```

Lead submissions POST to `VITE_LEAD_ENDPOINT` when set; otherwise they are stored in `localStorage` for booth collection. Demo video uses `VITE_DEMO_VIDEO_URL` when provided.
